# Server-only — the Next.js API routes in app/api/* read this to forward
# to the Python backend. NOT prefixed with NEXT_PUBLIC_, so it never ends
# up in the browser bundle.
BACKEND_URL=http://localhost:8080
